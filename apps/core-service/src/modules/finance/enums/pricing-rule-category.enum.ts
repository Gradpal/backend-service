export enum EPricingRuleCategory {
  SESSION_PRICE = 'session rice',
  CREDITS_PER_SESSION = 'credits per session',
  CREDITS_PER_BOOKING = 'credits per booking',
  CREDITS_PER_CANCELATION = 'credits per cancelation',
  CREDITS_PER_NO_SHOW = 'credits per no show',
  CREDITS_PER_LATE_CANCELLATION = 'credits per late cancellation',
}
